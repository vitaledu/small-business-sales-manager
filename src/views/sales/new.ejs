<div class="card">
  <h2>💳 Nova Venda</h2>

  <form onsubmit="registrarVenda(event)">
    <h3>Cliente</h3>
      <div>
      <label>
        Cliente:
        <select id="customerId" required>
          <option value="">-- Selecionar Cliente --</option>
          <option value="1">João da Silva</option>
          <option value="2">Loja Central</option>
        </select>
      </label>
    </div>

    <h3>Itens da Venda</h3>
    <table>
      <thead>
        <tr>
          <th>Produto</th>
          <th>Qtd</th>
          <th>Preço Unit.</th>
          <th>Desconto %</th>
          <th>Subtotal</th>
          <th>Ação</th>
        </tr>
      </thead>
      <tbody id="itemsTable">
        <tr>
          <td>
            <select required>
              <option value="">-- Selecionar --</option>
              <option value="1">Sacolé Morango (R$ 1,00)</option>
              <option value="2">Coca-Cola 2L (R$ 12,00)</option>
            </select>
          </td>
          <td><input type="number" value="1" min="1" placeholder="Qtd" required style="width: 60px;"></td>
          <td><input type="number" step="0.01" placeholder="Preço" required style="width: 80px;"></td>
          <td><input type="number" step="0.1" min="0" max="100" value="0" placeholder="%" style="width: 50px;"></td>
          <td class="currency">R$ 0,00</td>
          <td><button type="button" onclick="removerItem(this)">❌</button></td>
        </tr>
      </tbody>
    </table>
    <button type="button" onclick="adicionarItem()">+ Adicionar Item</button>

    <hr>

    <div class="form-row">
      <div>
        <label>Subtotal: <span id="subtotal" class="currency">R$ 0,00</span></label>
      </div>
      <div>
        <label>Desconto (R$): <input type="number" step="0.01" id="discountValue" value="0" style="width: 100px;"></label>
      </div>
      <div>
        <label>Depósito Garrafas: <span id="deposit" class="currency">R$ 0,00</span></label>
      </div>
    </div>

    <div style="background: #bee3f8; padding: 1rem; border-radius: 0.5rem; font-size: 1.25rem;">
      <strong>TOTAL: <span id="total" class="currency">R$ 0,00</span></strong>
    </div>

    <h3>Forma de Pagamento</h3>
    <div>
      <label><input type="radio" name="paymentMethod" value="DINHEIRO" required> Dinheiro</label>
      <label><input type="radio" name="paymentMethod" value="PIX"> PIX</label>
      <label><input type="radio" name="paymentMethod" value="CARTÃO"> Cartão</label>
    </div>

    <div class="btn-group">
      <button type="button" onclick="history.back()" class="btn">Cancelar</button>
      <button type="submit" class="btn" style="background: #48bb78; color: white;">✓ Finalizar Venda</button>
    </div>
  </form>
</div>

<script>
function adicionarItem() {
  alert('Adicionar novo item');
}
function removerItem(btn) {
  btn.closest('tr').remove();
}
function registrarVenda(e) {
  e.preventDefault();
  alert('Venda registrada!');
}
</script>
